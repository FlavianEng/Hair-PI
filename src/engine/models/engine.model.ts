/**
 * @enum {EOL} CR -> for old mac OS versions
 * @enum {EOL} CRFL -> for windows OS versions
 * @enum {EOL} LF -> for linux and mac OS versions
 */
export enum EOL {
  CR = "\r",
  CRLF = "\r\n",
  LF = "\n",
}
